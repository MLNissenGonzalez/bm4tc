# @package _global_
# ---------------------------------------------------------------------------
# Adversarial-training seed sweep (3 seeds) for mnist_full_r12 at legendre/d3r10c64,
# alpha=0. Fine-tunes from the best clean (alpha=0) NAT checkpoint.
#
# clean_weight=0.0, uniform across r10/r20/r40, from the cw_a0 grid HPO
# (hpo/clean_weight.yaml) selected on MAX rob/valid subject to acc_floor=0.90.
# Lower cw is better across the board: r20/r40 pick 0.0 outright, and r10's
# nominal best 0.1 edged 0.0 by <0.003 rob (within noise) -- so cw=0 is used
# everywhere for a clean bond-dim comparison. lr=3e-4 and soft_strength=1e-3
# are the fixed values used throughout the AT/NAT grids.
#
# RADIUS: eps_rel=0.1 (abs 0.2 in [-1,1]), the corrected training radius. The
# published a0_2507 sweep trained at eps_rel=0.3 (abs 0.6) -- the Madry 28x28
# MNIST convention, which does not transfer to 12x12 downsampled data where a
# given L-inf budget removes far more class information. The radius is the ONLY
# difference from a0_2507: batch_size, lr, soft_strength, clean_weight, curriculum
# and the selection rule are all unchanged, so this arm is a single-variable swap.
#
# 3 seeds, not 5: seed sd on the published AT sweeps is ~0.002-0.008 in rob/valid
# against bond-dim effects of 0.05-0.08, so n=3 costs ~0.001 of standard error.
# Runnable as-is (born, model_path, and all HPs are baked in):
#   python -m experiments.train --multirun \
#       +experiments=mnist_full_r12/at/legendre/d3r10c64/seed_sweep/a0
# ---------------------------------------------------------------------------
stage: seed_sweep
experiment: a0
descriptor: at_pretrained

defaults:
  - override /born: legendre/d3r10c64
  - override /dataset: mnist/mnist_full_r12
  - override /trainer/nll: null
  - override /trainer/adversarial: pgd_at
  - override /tracking: online

# Best clean (alpha=0) NAT checkpoint for this bond dim.
model_path: outputs/mnist_full_r12/nat/legendre/d3r10c64/seed_sweep/a0_2406/4/models/model

tracking:
  seed: 42
  evasion:
    norm: "inf"
    method: "PGD"
    criterion:
      name: "negative log-likelihood"
    eps_rel: [0.1]   # relative; abs eps = 0.1 x 2.0 = 0.2 in [-1,1]
    num_steps: 10
    step_size: null
    random_start: true

trainer:
  adversarial:
    alpha: 0.0
    max_epoch: 100
    batch_size: 512
    patience: 10
    stop_crit: "at_loss"
    eval_rob_freq: 5
    acc_floor: 0.90          # only select epochs whose clean valid-acc stays >= 90%
    save: true

    evasion:
      norm: "inf"
      criterion:
        name: "negative log-likelihood"
      eps_rel: [0.1]       # relative; abs eps = 0.2 in [-1,1]
      num_steps: 10
      step_size: null
      random_start: true

    curriculum: true
    curriculum_eps_start_rel: 0.01
    curriculum_end_epoch: 70

    auto_stack: true
    auto_unbind: false

    norm_control:
      hard_every: 0
      soft_strength: 1e-3    # FIXED -- flat over ~6 decades in the NAT studies
      log_target: null
      debug: false

    clean_weight: 0.0       # FIXED -- uniform cw=0 across bond dims (r10 best 0.1
                            # edged 0.0 by <0.003 rob, within noise; r20/r40 prefer 0.0)

    optimizer:
      name: "adam"
      kwargs:
        lr: 3e-4             # FIXED -- centre of the AT plateau (1.5e-4..7e-4)
        weight_decay: 0.0

hydra:
  sweeper:
    params:
      tracking.seed: range(1, 4)
