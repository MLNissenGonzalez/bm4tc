# @package _global_
experiment: seed_sweep

defaults:
  - override /born: legendre/d30r18
  - override /dataset: 2Dtoy/moons
  - override /trainer/adversarial: null
  - override /tracking: online

tracking:
  seed: 42
dataset:
  split_seed: 11
  gen_dow_kwargs:
    seed: 25

trainer:
  nll:
    max_epoch: 300
    batch_size: 256
    patience: 300
    stop_crit: "dis_loss"
    save: true
    optimizer:
      name: "adam"
      kwargs:
        lr: 0.01353930618155416    # FILL FROM HPO
        weight_decay: 0.000159856279233438  # FILL FROM HPO

hydra:
  sweeper:
    params:
      tracking.seed: range(1,21)
