# @package _global_
# Test configuration for adversarial training experiments
experiment: test

defaults:
  - override /born: fourier/test
  - override /dataset: test
  - override /trainer/nll: test
  - override /trainer/adversarial: test
  - override /tracking: test
