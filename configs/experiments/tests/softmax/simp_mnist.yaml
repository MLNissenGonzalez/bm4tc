# @package _global_
experiment: softmax_sanity_simp

defaults:
  - _self_
  - override /born: simp/d3r10
  - override /dataset: mnist/mnist_full
  - override /trainer/discriminative: softmax_adam50
  - override /trainer/ganstyle: null
  - override /trainer/adversarial: null
  - override /tracking: online

tracking:
  seed: 5
  mode: offline

trainer:
  classification:
    optimizer:
      kwargs:
        lr: 5e-5
