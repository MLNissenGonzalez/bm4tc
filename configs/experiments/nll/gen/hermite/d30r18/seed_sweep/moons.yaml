# @package _global_
experiment: seed_sweep

defaults:
  - override /born: hermite/d30r18
  - override /dataset: 2Dtoy/moons
  - override /trainer/nll: test
  - override /trainer/adversarial: null
  - override /tracking: online

tracking:
  seed: 42
dataset:
  split_seed: 11
  gen_dow_kwargs:
    seed: 25

trainer:
  nll:
    alpha: 1.0
    max_epoch: 150
    batch_size: 256
    patience: 150
    stop_crit: "gen_loss"
    save: true
    optimizer:
      name: "adam"
      kwargs:
        lr: 0.0008620810541196076    # FILL FROM HPO
        weight_decay: 1.7412492105979524e-05  # FILL FROM HPO

hydra:
  sweeper:
    params:
      tracking.seed: range(1,21)
