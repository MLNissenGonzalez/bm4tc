init_kwargs:
  in_dim: 3 # physical dimension
  bond_dim: 10
  boundary: "obc"
  init_method: "randn_eye"
  std: 1e-9
  dtype: "complex64"
embedding: "fourier"
accumulate: true   # realistic high-feature dataset -> overflow-safe path on by default
