init_kwargs:
  in_dim: 10 # physical dimension
  bond_dim: 6
  boundary: "obc"
  init_method: "canonical"
  dtype: "complex64"
embedding: "hermite"
accumulate: true   # complex64 -> overflow-safe path on (see CLAUDE.md, Key invariants)
