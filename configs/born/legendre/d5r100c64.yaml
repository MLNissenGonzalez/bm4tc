init_kwargs:
  in_dim: 5 # physical dimension
  bond_dim: 100
  boundary: "obc"
  init_method: "randn_eye"
  std: 1e-9
  dtype: "complex64"
embedding: "legendre"
accumulate: true   # realistic high-feature dataset -> overflow-safe path on by default
