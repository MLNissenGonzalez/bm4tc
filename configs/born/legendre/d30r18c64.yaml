init_kwargs:
  in_dim: 30 # physical dimension
  bond_dim: 18
  boundary: "obc"
  init_method: "randn_eye"
  std: 1e-9
  dtype: "complex64"
embedding: "legendre"
accumulate: true   # complex64 -> overflow-safe path on (see CLAUDE.md, Key invariants)
