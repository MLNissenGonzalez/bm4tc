# Old environment not needed for repo
environment_old.*

# I don't know I want to keep it
spec_file.txt

# Save images only locally.
images/

# Generated figures (reproduced by the notebooks under notebooks/)
# figures/

# Track notebooks/ but keep the personal-experimentation archive out of git
notebooks/archive/

# Experiment run outputs: logging, images, model parameters. Nothing here is
# worth versioning -- checkpoints were never tracked anyway (see `models/` and
# `*.pt` below), and the resolved Hydra configs are reproducible from configs/.
outputs/
multirun/

# PyTorch
models/
*.pt
*.pth

# Checkpoints and logs
checkpoints/
logs/
*.log
log.*

# Data on disk
.datasets/

# Jupyter
.ipynb_checkpoints

# Python
__pycache__/
*.pyc

# Debugging
experiments/debug.py
.vscode/

# Tracking
wandb/
experiments/wandb_test.py

# scratchpad
maxBond_2d.py

# archive (old version of of the code (pre refactoring in object oriented coding))
archive/

# Analysis outputs (evaluation CSVs, summaries, figures -- all regenerated by
# analysis/sweep.py, analysis/gibbs.py and analysis/visualize/). Kept out of main
# so re-running a sweep doesn't rewrite multi-MB CSVs into permanent history.
# They are versioned on the disposable `results` branch instead; see
# tools/publish_results.sh.
analysis/outputs/
# analysis/*.png
# analysis/scratchbook.ipynb

# github
.github/
.github
/mathqi/mnissen/adversarial_training_mps/.github
.claude/
.vscode/

tutorial_mnist_script.py
data/

# claude
$HOME/

.pytest_cache/
pytest.ini
reference/
DEFERRED.md
CLAUDE.md
