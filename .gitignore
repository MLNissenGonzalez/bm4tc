# Old environment not needed for repo
environment_old.*

# I don't know I want to keep it
spec_file.txt

# Save images only locally.
images/

# Generated figures (reproduced by the notebooks under notebooks/)
# figures/

# Track notebooks/ but keep the personal-experimentation archive out of git
notebooks/archive/

# Experiment run outputs: logging, images, model parameters. Nothing here is
# worth versioning -- checkpoints were never tracked anyway (see `models/` and
# `*.pt` below), and the resolved Hydra configs are reproducible from configs/.
# Leading slash anchors this to the top-level dir only -- a bare `outputs/`
# would also swallow analysis/outputs/, which IS tracked.
/outputs/
multirun/

# PyTorch
models/
*.pt
*.pth

# Checkpoints and logs
checkpoints/
logs/
*.log
log.*

# Data on disk
.datasets/

# Jupyter
.ipynb_checkpoints

# Python
__pycache__/
*.pyc

# Debugging
experiments/debug.py
.vscode/

# Tracking
wandb/
experiments/wandb_test.py

# scratchpad
maxBond_2d.py

# archive (old version of of the code (pre refactoring in object oriented coding))
archive/

# Analysis outputs (evaluation CSVs, summaries, figures) are TRACKED on main --
# they are the results and must be backed up by an ordinary commit + push.
# Deliberately not ignored: git never touches ignored files, but it also never
# saves them.
# analysis/outputs/
# analysis/*.png
# analysis/scratchbook.ipynb

# github
.github/
.github
/mathqi/mnissen/adversarial_training_mps/.github
.claude/
.vscode/

tutorial_mnist_script.py
data/

# claude
$HOME/

.pytest_cache/
pytest.ini
reference/
DEFERRED.md
CLAUDE.md
